import { StyleSheet } from "react-native";

export default StyleSheet.create({
  header: {
    backgroundColor: "green"
  },
  headerTitle: {
      color:"white",
      textAlign: "center"
  },
  headerButton:{
    marginLeft:10
  }
});
